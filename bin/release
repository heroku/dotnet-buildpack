#!/usr/bin/env bash
# bin/release <build-dir>

cat <<EOF
---
config_vars:
  PATH: /app/mono/bin:/app/xsp/bin:/usr/local/bin:/usr/bin:/bin
default_process_types:
  web: /app/xsp/bin/xsp4 --port $PORT
EOF
